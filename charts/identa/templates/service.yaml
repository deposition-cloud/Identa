apiVersion: v1
kind: Service
metadata:
  name: {{ include "identa.fullname" . }}
  labels:
    {{- include "identa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identa.selectorLabels" . | nindent 4 }}
